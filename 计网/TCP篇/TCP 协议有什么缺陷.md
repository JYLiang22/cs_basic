- [升级 TCP 的工作很困难](#升级-tcp-的工作很困难)
- [TCP 建立连接的延迟](#tcp-建立连接的延迟)
- [TCP 存在队头阻塞问题](#tcp-存在队头阻塞问题)
- [网络迁移需要重新建立 TCP 连接](#网络迁移需要重新建立-tcp-连接)
- [结尾](#结尾)


# 升级 TCP 的工作很困难
主要就是一个牵一发而动全身，想要单纯升级TCP还是不难的，难的是更新配套的软件、以及操作系统。


# TCP 建立连接的延迟
1. 延迟来源：现在大多数网站都是使用 HTTPS 的，这意味着在 TCP 三次握手之后，还需要经过 TLS 四次握手后，才能进行 HTTP 数据的传输，这在一定程序上增加了数据传输的延迟
2. 基于安全和成本问题而不得不进行三次握手


# TCP 存在队头阻塞问题
搞清楚什么是队头阻塞问题，为什么会出现这个问题。


# 网络迁移需要重新建立 TCP 连接
从建立TCP连接的四元组进行理解，因为网络迁移导致四元组的某个元变了，所以要重连。


# 结尾
这部分说已有的QUIC协议已经把上述缺点都改进了，而且已经应用在HTTP3上。
