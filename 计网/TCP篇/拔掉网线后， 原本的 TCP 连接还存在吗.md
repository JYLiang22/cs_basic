- [拔掉网线后有数据传输](#拔掉网线后有数据传输)
- [拔掉网线后没有数据传输](#拔掉网线后没有数据传输)


先上结论：拔掉网线后，TCP连接状态不会收到影响，是否存在取决于是否有数据传输。因为TCP连接在Linux内核中是一个名为struct socket的结构体，拔掉网线是，操作系统不会改变该结构体的任何内容，所以tcp连接的状态也不会发生改变。<br>
这篇也直接看结论行了，因为有了前面的基础，自己也能分析出来大半了。


# 拔掉网线后有数据传输



# 拔掉网线后没有数据传输